{
  "name": "@desk/plugin-sdk",
  "version": "0.1.0",
  "description": "The plugin contract and runtime registry for Desk. Build new artifact types, component types, or relation types against this package.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@desk/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "publishConfig": {
    "access": "public"
  }
}
